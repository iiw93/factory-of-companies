# Acceptance Checklist — Deployment Target Contract

## AC-2401
Deployment target schema exists.

## AC-2402
`deployment_target_id` is required.

## AC-2403
`target_name` is required.

## AC-2404
`target_type` enum is enforced.

## AC-2405
`target_status` enum is enforced.

## AC-2406
`created_at` is required.

## AC-2407
`project_id` is optional.

## AC-2408
`company_id` is optional.

## AC-2409
`linked_delivery_package_id` is optional.

## AC-2410
`linked_release_decision_id` is optional.

## AC-2411
`environment_name` enum is optional.

## AC-2412
`target_uri` is optional.

## AC-2413
`target_note` is optional.

## AC-2414
Delivery/release/project/company linkage remains aligned.

## AC-2415
Docs/specs/schema remain aligned.
