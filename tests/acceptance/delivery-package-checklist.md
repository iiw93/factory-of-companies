# Acceptance Checklist — Delivery Package Contract

## AC-2301
Delivery package schema exists.

## AC-2302
`delivery_package_id` is required.

## AC-2303
`package_name` is required.

## AC-2304
`package_status` enum is enforced.

## AC-2305
`created_at` is required.

## AC-2306
`project_id` is optional.

## AC-2307
`trace_id` is optional.

## AC-2308
`artifact_ids` is optional.

## AC-2309
`linked_evidence_bundle_id` is optional.

## AC-2310
`linked_release_decision_id` is optional.

## AC-2311
`linked_execution_result_id` is optional.

## AC-2312
`package_uri` is optional.

## AC-2313
`package_note` is optional.

## AC-2314
Release/evidence/artifact/result linkage remains aligned.

## AC-2315
Docs/specs/schema remain aligned.
