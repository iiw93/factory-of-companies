# Acceptance Checklist — Orchestration Handoff Contract

## AC-1801
Orchestration handoff schema exists.

## AC-1802
`handoff_id` is required.

## AC-1803
`trace_id` is required.

## AC-1804
`command_id` is required.

## AC-1805
`source_role` is required.

## AC-1806
`target_role` is required.

## AC-1807
`action_type` is required.

## AC-1808
`handoff_status` enum is enforced.

## AC-1809
`priority` enum is enforced.

## AC-1810
`created_at` is required.

## AC-1811
`execution_request_id` is optional.

## AC-1812
`linked_governance_decision_id` is optional.

## AC-1813
`linked_artifact_id` is optional.

## AC-1814
`handoff_note` is optional.

## AC-1815
Role/action/decision linkage remains aligned.

## AC-1816
Docs/specs/schema remain aligned.
