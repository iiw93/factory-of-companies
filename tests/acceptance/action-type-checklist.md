# Acceptance Checklist — Action Type Contract

## AC-801
Action type schema exists.

## AC-802
`action_type_id` is required.

## AC-803
`action_name` is required.

## AC-804
`action_type` is required.

## AC-805
`description` is required.

## AC-806
`recommended_roles` is optional.

## AC-807
`action_type` enum is enforced.

## AC-808
Execution request `action_type` stays aligned with action type contract.

## AC-809
Agent role `allowed_action_types` stays aligned conceptually.

## AC-810
Docs/specs/schema remain aligned.
