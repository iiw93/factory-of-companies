# Acceptance Checklist — Tool Invocation Contract

## AC-2601
Tool invocation schema exists.

## AC-2602
`tool_invocation_id` is required.

## AC-2603
`tool_name` is required.

## AC-2604
`tool_type` enum is enforced.

## AC-2605
`invocation_status` enum is enforced.

## AC-2606
`created_at` is required.

## AC-2607
`trace_id` is optional.

## AC-2608
`execution_request_id` is optional.

## AC-2609
`execution_result_id` is optional.

## AC-2610
`handoff_id` is optional.

## AC-2611
`role_type` is optional.

## AC-2612
`action_type` is optional.

## AC-2613
`input_reference` is optional.

## AC-2614
`output_reference` is optional.

## AC-2615
`tool_note` is optional.

## AC-2616
Execution/handoff/role/action linkage remains aligned.

## AC-2617
Docs/specs/schema remain aligned.
