# Acceptance Checklist - Embedding Job Contract

## AC-3501
Embedding job schema exists.

## AC-3502
`embedding_job_id` is required.

## AC-3503
`job_name` is required.

## AC-3504
`job_status` enum is enforced.

## AC-3505
`created_at` is required.

## AC-3506
`embedding_provider_id` is required.

## AC-3507
`knowledge_source_ids` is optional.

## AC-3508
`trace_id` is optional.

## AC-3509
`linked_knowledge_retrieval_id` is optional.

## AC-3510
`linked_retrieval_session_id` is optional.

## AC-3511
`modality_scope` enum is optional.

## AC-3512
`output_dimension` is optional.

## AC-3513
`chunk_count` is optional.

## AC-3514
`job_note` is optional.

## AC-3515
Provider/source/retrieval linkage remains aligned.

## AC-3516
Docs/specs/schema remain aligned.
