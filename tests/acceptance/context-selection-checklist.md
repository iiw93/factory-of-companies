# Acceptance Checklist — Context Selection Contract

## AC-2801
Context selection schema exists.

## AC-2802
`context_selection_id` is required.

## AC-2803
`selection_name` is required.

## AC-2804
`selection_status` enum is enforced.

## AC-2805
`created_at` is required.

## AC-2806
`trace_id` is optional.

## AC-2807
`execution_request_id` is optional.

## AC-2808
`selected_source_ids` is optional.

## AC-2809
`selection_strategy` enum is optional.

## AC-2810
`linked_tool_invocation_id` is optional.

## AC-2811
`linked_evidence_bundle_id` is optional.

## AC-2812
`selection_note` is optional.

## AC-2813
Knowledge/tool/evidence/request linkage remains aligned.

## AC-2814
Docs/specs/schema remain aligned.
