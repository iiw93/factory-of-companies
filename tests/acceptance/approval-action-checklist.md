# Acceptance Checklist — Approval Action Contract

## AC-401
Approval action schema exists.

## AC-402
`approval_action_id` is required.

## AC-403
`trace_id` is required.

## AC-404
`command_id` is required.

## AC-405
`user_id` is required.

## AC-406
`session_id` is required.

## AC-407
`decision` enum is enforced.

## AC-408
`created_at` is required.

## AC-409
`reason` is optional.

## AC-410
`comment` is optional.

## AC-411
Docs/specs/schema remain aligned.
