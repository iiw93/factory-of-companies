# Acceptance Checklist — Owner Identity Contract

## AC-1501
Owner identity schema exists.

## AC-1502
`user_id` is required.

## AC-1503
`display_name` is required.

## AC-1504
`identity_type` enum is enforced.

## AC-1505
`created_at` is required.

## AC-1506
`primary_channel` enum is enforced.

## AC-1507
`updated_at` is optional.

## AC-1508
`telegram_user_id` is optional.

## AC-1509
`dashboard_user_id` is optional.

## AC-1510
`default_company_id` is optional.

## AC-1511
`default_project_id` is optional.

## AC-1512
`identity_note` is optional.

## AC-1513
Session/project/company/approval user linkage remains aligned.

## AC-1514
Docs/specs/schema remain aligned.
