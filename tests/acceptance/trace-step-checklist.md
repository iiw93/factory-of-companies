# Acceptance Checklist - Trace Step Contract

## AC-3901
Trace step schema exists.

## AC-3902
`trace_step_id` is required.

## AC-3903
`step_name` is required.

## AC-3904
`step_type` enum is enforced.

## AC-3905
`step_status` enum is enforced.

## AC-3906
`created_at` is required.

## AC-3907
`trace_id` is required.

## AC-3908
`sequence_order` is required.

## AC-3909
`source_contract` is required.

## AC-3910
`source_id` is required.

## AC-3911
`linked_event_ids` is optional.

## AC-3912
`linked_embedding_job_id` is optional.

## AC-3913
`linked_retrieval_index_id` is optional.

## AC-3914
`linked_knowledge_retrieval_id` is optional.

## AC-3915
`linked_retrieval_session_id` is optional.

## AC-3916
`linked_retrieval_result_id` is optional.

## AC-3917
`step_message` is optional.

## AC-3918
`step_note` is optional.

## AC-3919
Trace/event/indexing linkage remains aligned.

## AC-3920
Docs/specs/schema remain aligned.
