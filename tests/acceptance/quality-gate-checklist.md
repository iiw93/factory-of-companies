# Acceptance Checklist — Quality Gate Contract

## AC-2001
Quality gate schema exists.

## AC-2002
`quality_gate_id` is required.

## AC-2003
`gate_name` is required.

## AC-2004
`gate_type` enum is enforced.

## AC-2005
`gate_status` enum is enforced.

## AC-2006
`created_at` is required.

## AC-2007
`trace_id` is optional.

## AC-2008
`project_id` is optional.

## AC-2009
`artifact_id` is optional.

## AC-2010
`execution_result_id` is optional.

## AC-2011
`linked_governance_decision_id` is optional.

## AC-2012
`evidence_uri` is optional.

## AC-2013
`gate_note` is optional.

## AC-2014
Artifact/result/governance linkage remains aligned.

## AC-2015
Docs/specs/schema remain aligned.

