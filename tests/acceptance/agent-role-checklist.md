# Acceptance Checklist — Agent Role Contract

## AC-701
Agent role schema exists.

## AC-702
`role_id` is required.

## AC-703
`role_name` is required.

## AC-704
`role_type` is required.

## AC-705
`description` is required.

## AC-706
`allowed_action_types` is optional.

## AC-707
`role_type` enum is enforced.

## AC-708
Execution request `target_role` stays aligned with declared role model.

## AC-709
Docs/specs/schema remain aligned.
