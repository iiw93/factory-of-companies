# Acceptance Checklist — Knowledge Retrieval Contract

## AC-3101
Knowledge retrieval schema exists.

## AC-3102
`knowledge_retrieval_id` is required.

## AC-3103
`contract_version` is required.

## AC-3104
`retrieval_status` enum is enforced.

## AC-3105
`retrieval_mode` enum is enforced.

## AC-3106
`query_text` is required.

## AC-3107
`created_at` is required.

## AC-3108
`trace_id` is optional.

## AC-3109
`execution_request_id` is optional.

## AC-3110
`context_selection_id` is optional.

## AC-3111
`selected_source_ids` is optional.

## AC-3112
`retrieved_artifact_ids` is optional.

## AC-3113
`retrieval_constraints` is optional.

## AC-3114
`ranking_hint` is optional.

## AC-3115
`token_budget_hint` is optional.

## AC-3116
Context/source/artifact/request linkage remains aligned.

## AC-3117
Docs/specs/schema remain aligned.
