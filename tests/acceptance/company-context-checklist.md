# Acceptance Checklist — Company Context Contract

## AC-1401
Company context schema exists.

## AC-1402
`company_id` is required.

## AC-1403
`company_name` is required.

## AC-1404
`company_status` enum is enforced.

## AC-1405
`created_at` is required.

## AC-1406
`owner_user_id` is required.

## AC-1407
`updated_at` is optional.

## AC-1408
`active_project_id` is optional.

## AC-1409
`active_session_id` is optional.

## AC-1410
`active_trace_id` is optional.

## AC-1411
`company_note` is optional.

## AC-1412
Session/project/traceability company linkage remains aligned.

## AC-1413
Docs/specs/schema remain aligned.
