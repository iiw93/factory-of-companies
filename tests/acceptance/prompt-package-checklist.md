# Acceptance Checklist — Prompt Package Contract

## AC-2901
Prompt package schema exists.

## AC-2902
`prompt_package_id` is required.

## AC-2903
`package_name` is required.

## AC-2904
`package_status` enum is enforced.

## AC-2905
`created_at` is required.

## AC-2906
`instruction_text` is required.

## AC-2907
`trace_id` is optional.

## AC-2908
`execution_request_id` is optional.

## AC-2909
`context_selection_id` is optional.

## AC-2910
`role_type` is optional.

## AC-2911
`action_type` is optional.

## AC-2912
`selected_source_ids` is optional.

## AC-2913
`linked_tool_invocation_id` is optional.

## AC-2914
`package_note` is optional.

## AC-2915
Context/knowledge/tool/request linkage remains aligned.

## AC-2916
Docs/specs/schema remain aligned.
