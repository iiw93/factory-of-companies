# Acceptance Checklist — Runtime Capability Contract

## AC-2501
Runtime capability schema exists.

## AC-2502
`capability_id` is required.

## AC-2503
`capability_name` is required.

## AC-2504
`capability_status` enum is enforced.

## AC-2505
`created_at` is required.

## AC-2506
`role_type` enum is enforced.

## AC-2507
`deployment_target_id` is optional.

## AC-2508
`supported_action_types` is optional.

## AC-2509
Capability flags are optional booleans.

## AC-2510
`capability_note` is optional.

## AC-2511
Role/action/deployment linkage remains aligned.

## AC-2512
Docs/specs/schema remain aligned.
