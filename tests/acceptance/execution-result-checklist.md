# Acceptance Checklist — Execution Result Contract

## AC-601
Execution result schema exists.

## AC-602
`execution_result_id` is required.

## AC-603
`execution_request_id` is required.

## AC-604
`trace_id` is required.

## AC-605
`command_id` is required.

## AC-606
`session_id` is required.

## AC-607
`user_id` is required.

## AC-608
`outcome` enum is enforced.

## AC-609
`created_at` is required.

## AC-610
`completed_at` is optional.

## AC-611
`result_payload` is optional.

## AC-612
`error_code` is optional.

## AC-613
`error_message` is optional.

## AC-614
Docs/specs/schema remain aligned.
