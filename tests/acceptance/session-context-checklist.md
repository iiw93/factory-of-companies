# Acceptance Checklist — Session Context Contract

## AC-1201
Session context schema exists.

## AC-1202
`session_id` is required.

## AC-1203
`user_id` is required.

## AC-1204
`channel` enum is enforced.

## AC-1205
`session_status` enum is enforced.

## AC-1206
`created_at` is required.

## AC-1207
`updated_at` is optional.

## AC-1208
`active_trace_id` is optional.

## AC-1209
`project_id` is optional.

## AC-1210
`company_id` is optional.

## AC-1211
`context_note` is optional.

## AC-1212
Traceability and command/response session linkage remain aligned.

## AC-1213
Docs/specs/schema remain aligned.
