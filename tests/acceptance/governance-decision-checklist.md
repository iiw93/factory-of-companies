# Acceptance Checklist — Governance Decision Contract

## AC-1701
Governance decision schema exists.

## AC-1702
`governance_decision_id` is required.

## AC-1703
`trace_id` is required.

## AC-1704
`command_id` is required.

## AC-1705
`user_id` is required.

## AC-1706
`decision_type` enum is enforced.

## AC-1707
`decision_scope` enum is enforced.

## AC-1708
`created_at` is required.

## AC-1709
`execution_request_id` is optional.

## AC-1710
`linked_approval_action_id` is optional.

## AC-1711
`linked_budget_hint_id` is optional.

## AC-1712
`linked_timeout_policy_id` is optional.

## AC-1713
`rationale` is optional.

## AC-1714
`decision_note` is optional.

## AC-1715
Governance linkage to approval/budget/timeout/request remains aligned.

## AC-1716
Docs/specs/schema remain aligned.
