# Acceptance Checklist — Retrieval Session Contract

## AC-3301
Retrieval session schema exists.

## AC-3302
`retrieval_session_id` is required.

## AC-3303
`session_name` is required.

## AC-3304
`session_status` enum is enforced.

## AC-3305
`created_at` is required.

## AC-3306
`trace_id` is optional.

## AC-3307
`execution_request_id` is optional.

## AC-3308
`context_selection_id` is optional.

## AC-3309
`linked_knowledge_retrieval_id` is optional.

## AC-3310
`linked_prompt_package_id` is optional.

## AC-3311
`selected_source_ids` is optional.

## AC-3312
`retrieval_outcome` enum is optional.

## AC-3313
`session_note` is optional.

## AC-3314
Retrieval/context/prompt/request linkage remains aligned.

## AC-3315
Docs/specs/schema remain aligned.
