# Acceptance Checklist — Project Context Contract

## AC-1301
Project context schema exists.

## AC-1302
`project_id` is required.

## AC-1303
`project_name` is required.

## AC-1304
`project_status` enum is enforced.

## AC-1305
`created_at` is required.

## AC-1306
`owner_user_id` is required.

## AC-1307
`updated_at` is optional.

## AC-1308
`company_id` is optional.

## AC-1309
`active_session_id` is optional.

## AC-1310
`active_trace_id` is optional.

## AC-1311
`project_note` is optional.

## AC-1312
Session/traceability/execution project linkage remains aligned.

## AC-1313
Docs/specs/schema remain aligned.
