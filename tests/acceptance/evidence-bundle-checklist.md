# Acceptance Checklist — Evidence Bundle Contract

## AC-2101
Evidence bundle schema exists.

## AC-2102
`evidence_bundle_id` is required.

## AC-2103
`bundle_name` is required.

## AC-2104
`bundle_status` enum is enforced.

## AC-2105
`created_at` is required.

## AC-2106
`trace_id` is optional.

## AC-2107
`project_id` is optional.

## AC-2108
`linked_quality_gate_id` is optional.

## AC-2109
`linked_execution_result_id` is optional.

## AC-2110
`linked_governance_decision_id` is optional.

## AC-2111
`artifact_ids` is optional.

## AC-2112
`evidence_summary` is optional.

## AC-2113
`bundle_note` is optional.

## AC-2114
Artifact / quality / governance / result linkage remains aligned.

## AC-2115
Docs/specs/schema remain aligned.
