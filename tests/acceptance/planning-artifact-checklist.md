# Acceptance Checklist — Planning Artifact Contract

## AC-1901
Planning artifact schema exists.

## AC-1902
`planning_artifact_id` is required.

## AC-1903
`artifact_id` is required.

## AC-1904
`project_id` is required.

## AC-1905
`trace_id` is required.

## AC-1906
`planning_type` enum is enforced.

## AC-1907
`source_role` is required.

## AC-1908
`planning_status` enum is enforced.

## AC-1909
`planning_horizon` enum is enforced.

## AC-1910
`created_at` is required.

## AC-1911
`artifact_uri` is required.

## AC-1912
`planning_note` is optional.

## AC-1913
Artifact / project / trace / role linkage remains aligned.

## AC-1914
Docs/specs/schema remain aligned.
