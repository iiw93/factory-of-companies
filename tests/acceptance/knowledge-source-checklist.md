# Acceptance Checklist — Knowledge Source Contract

## AC-2701
Knowledge source schema exists.

## AC-2702
`knowledge_source_id` is required.

## AC-2703
`source_name` is required.

## AC-2704
`source_type` enum is enforced.

## AC-2705
`source_status` enum is enforced.

## AC-2706
`created_at` is required.

## AC-2707
`artifact_id` is optional.

## AC-2708
`trace_id` is optional.

## AC-2709
`project_id` is optional.

## AC-2710
`linked_tool_invocation_id` is optional.

## AC-2711
`linked_evidence_bundle_id` is optional.

## AC-2712
`source_uri` is optional.

## AC-2713
`source_note` is optional.

## AC-2714
Artifact/tool/evidence/project linkage remains aligned.

## AC-2715
Docs/specs/schema remain aligned.
