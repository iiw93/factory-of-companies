# Acceptance Checklist - Embedding Provider Contract

## AC-3201
Embedding provider schema exists.

## AC-3202
`embedding_provider_id` is required.

## AC-3203
`provider_name` is required.

## AC-3204
`provider_type` enum is enforced.

## AC-3205
`provider_status` enum is enforced.

## AC-3206
`created_at` is required.

## AC-3207
Modality flags are optional booleans.

## AC-3208
`default_dimension` is optional.

## AC-3209
`max_dimension` is optional.

## AC-3210
`provider_note` is optional.

## AC-3211
Gemini embedding is represented as provider, not generative router.

## AC-3212
Docs/specs/schema remain aligned.
