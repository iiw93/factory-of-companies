# Acceptance Checklist — Budget Hint Contract

## AC-901
Budget hint schema exists.

## AC-902
`budget_hint_id` is required.

## AC-903
`trace_id` is required.

## AC-904
`command_id` is required.

## AC-905
`budget_amount` is required.

## AC-906
`budget_unit` enum is enforced.

## AC-907
`created_at` is required.

## AC-908
`scope` enum is enforced.

## AC-909
`note` is optional.

## AC-910
Execution request `budget_hint` stays aligned conceptually.

## AC-911
Docs/specs/schema remain aligned.
