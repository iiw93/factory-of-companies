# Acceptance Checklist - Observability Event Contract

## AC-3801
Observability event schema exists.

## AC-3802
`observability_event_id` is required.

## AC-3803
`event_name` is required.

## AC-3804
`event_type` enum is enforced.

## AC-3805
`event_status` enum is enforced.

## AC-3806
`created_at` is required.

## AC-3807
`source_contract` is required.

## AC-3808
`source_id` is required.

## AC-3809
`stage_name` is required.

## AC-3810
`severity` enum is enforced.

## AC-3811
`trace_id` is optional.

## AC-3812
`linked_embedding_job_id` is optional.

## AC-3813
`linked_retrieval_index_id` is optional.

## AC-3814
`linked_knowledge_retrieval_id` is optional.

## AC-3815
`linked_retrieval_session_id` is optional.

## AC-3816
`linked_retrieval_result_id` is optional.

## AC-3817
`event_message` is optional.

## AC-3818
`event_note` is optional.

## AC-3819
Event linkage remains aligned with indexing and retrieval contracts.

## AC-3820
Docs/specs/schema remain aligned.
