# Acceptance Checklist — Retrieval Result Contract

## AC-3401
Retrieval result schema exists.

## AC-3402
`retrieval_result_id` is required.

## AC-3403
`result_name` is required.

## AC-3404
`result_status` enum is enforced.

## AC-3405
`created_at` is required.

## AC-3406
`trace_id` is optional.

## AC-3407
`execution_request_id` is optional.

## AC-3408
`context_selection_id` is optional.

## AC-3409
`linked_knowledge_retrieval_id` is optional.

## AC-3410
`linked_retrieval_session_id` is optional.

## AC-3411
`linked_prompt_package_id` is optional.

## AC-3412
`resolved_source_ids` is optional.

## AC-3413
`result_quality` enum is optional.

## AC-3414
`selected_excerpt_count` is optional.

## AC-3415
`result_note` is optional.

## AC-3416
Retrieval/session/context/prompt linkage remains aligned.

## AC-3417
Docs/specs/schema remain aligned.
