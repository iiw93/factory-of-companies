# Acceptance Checklist — Traceability Envelope

## AC-301
Traceability schema exists.

## AC-302
`trace_id` is required.

## AC-303
`command_id` is required.

## AC-304
`session_id` is required.

## AC-305
`user_id` is required.

## AC-306
`channel` enum is enforced.

## AC-307
`current_state` enum is enforced.

## AC-308
`response_id` is optional.

## AC-309
Envelope links command and response conceptually.

## AC-310
Docs/specs/schema remain aligned.
