# Acceptance Checklist — Release Decision Contract

## AC-2201
Release decision schema exists.

## AC-2202
`release_decision_id` is required.

## AC-2203
`decision_name` is required.

## AC-2204
`release_status` enum is enforced.

## AC-2205
`created_at` is required.

## AC-2206
`trace_id` is optional.

## AC-2207
`project_id` is optional.

## AC-2208
`execution_result_id` is optional.

## AC-2209
`artifact_id` is optional.

## AC-2210
`linked_quality_gate_id` is optional.

## AC-2211
`linked_evidence_bundle_id` is optional.

## AC-2212
`linked_governance_decision_id` is optional.

## AC-2213
`release_scope` enum is optional.

## AC-2214
`release_note` is optional.

## AC-2215
Quality/evidence/governance/result linkage remains aligned.

## AC-2216
Docs/specs/schema remain aligned.
