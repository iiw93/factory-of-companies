# Acceptance Checklist — Execution Request Contract

## AC-501
Execution request schema exists.

## AC-502
`execution_request_id` is required.

## AC-503
`trace_id` is required.

## AC-504
`command_id` is required.

## AC-505
`session_id` is required.

## AC-506
`user_id` is required.

## AC-507
`target_role` is required.

## AC-508
`action_type` is required.

## AC-509
`priority` enum is enforced.

## AC-510
`created_at` is required.

## AC-511
`project_id` is optional.

## AC-512
`timeout_seconds` is optional.

## AC-513
`budget_hint` is optional.

## AC-514
`payload` is optional.

## AC-515
Docs/specs/schema remain aligned.
