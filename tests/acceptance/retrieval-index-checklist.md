# Acceptance Checklist - Retrieval Index Contract

## AC-3601
Retrieval index schema exists.

## AC-3602
`retrieval_index_id` is required.

## AC-3603
`index_name` is required.

## AC-3604
`index_status` enum is enforced.

## AC-3605
`created_at` is required.

## AC-3606
`embedding_provider_id` is required.

## AC-3607
`linked_embedding_job_ids` is optional.

## AC-3608
`knowledge_source_ids` is optional.

## AC-3609
`modality_scope` enum is optional.

## AC-3610
`dimension` is optional.

## AC-3611
`index_backend` enum is optional.

## AC-3612
`record_count` is optional.

## AC-3613
`index_note` is optional.

## AC-3614
Provider/job/source/index linkage remains aligned.

## AC-3615
Docs/specs/schema remain aligned.
